[project]
name = "codestra-social"
version = "0.1.0"
description = "Codestra enterprise social control plane"
requires-python = ">=3.12"
dependencies = ["fastapi>=0.116,<1", "uvicorn[standard]>=0.35,<1", "pydantic>=2.11,<3"]
